CREATE TABLE IF NOT EXISTS system_config (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  config_key VARCHAR(128) NOT NULL UNIQUE,
  config_value TEXT NOT NULL,
  config_type VARCHAR(32) NOT NULL DEFAULT 'string',
  config_group VARCHAR(64) NOT NULL DEFAULT 'default',
  remark VARCHAR(255),
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS user_account (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  user_no VARCHAR(64) NOT NULL UNIQUE,
  phone VARCHAR(32) NOT NULL UNIQUE,
  password_hash VARCHAR(255),
  nickname VARCHAR(64) NOT NULL,
  avatar_url VARCHAR(512),
  status VARCHAR(32) NOT NULL DEFAULT 'ACTIVE',
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS admin_user_permission (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  user_id BIGINT NOT NULL,
  permission_code VARCHAR(64) NOT NULL,
  enabled BOOLEAN NOT NULL DEFAULT TRUE,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  UNIQUE(user_id, permission_code)
);

CREATE INDEX IF NOT EXISTS idx_admin_user_permission_user ON admin_user_permission(user_id, enabled);

CREATE TABLE IF NOT EXISTS admin_session (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  session_id VARCHAR(128) NOT NULL UNIQUE,
  user_id BIGINT NOT NULL,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  expires_at TIMESTAMP NOT NULL,
  revoked BOOLEAN NOT NULL DEFAULT FALSE
);

CREATE INDEX IF NOT EXISTS idx_admin_session_user ON admin_session(user_id, revoked, expires_at);

CREATE TABLE IF NOT EXISTS user_session (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  access_token VARCHAR(128) NOT NULL UNIQUE,
  refresh_token VARCHAR(128) NOT NULL UNIQUE,
  user_id BIGINT NOT NULL,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  expires_at TIMESTAMP NOT NULL,
  revoked BOOLEAN NOT NULL DEFAULT FALSE
);

CREATE INDEX IF NOT EXISTS idx_user_session_user ON user_session(user_id, revoked, expires_at);

CREATE TABLE IF NOT EXISTS user_profile (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  user_id BIGINT NOT NULL UNIQUE,
  gender VARCHAR(16),
  city VARCHAR(64),
  bio VARCHAR(512),
  identity_status VARCHAR(32) NOT NULL DEFAULT 'UNVERIFIED',
  main_role VARCHAR(32) NOT NULL DEFAULT 'BUYER',
  video_identity_status VARCHAR(32) NOT NULL DEFAULT 'UNVERIFIED',
  video_verified BOOLEAN NOT NULL DEFAULT FALSE,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS user_follow (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  follower_id BIGINT NOT NULL,
  followed_id BIGINT NOT NULL,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  UNIQUE(follower_id, followed_id)
);

CREATE TABLE IF NOT EXISTS product_item (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  product_no VARCHAR(128) NOT NULL UNIQUE,
  seller_id BIGINT NOT NULL,
  title VARCHAR(128) NOT NULL,
  category VARCHAR(64) NOT NULL,
  price DECIMAL(18,2) NOT NULL,
  product_status VARCHAR(32) NOT NULL DEFAULT 'PENDING_AUDIT',
  audit_status VARCHAR(32) NOT NULL DEFAULT 'PENDING',
  visible BOOLEAN NOT NULL DEFAULT FALSE,
  trade_rule VARCHAR(128) NOT NULL DEFAULT 'offline-face-to-face-after-platform-order',
  description TEXT,
  image_urls TEXT,
  locked_order_no VARCHAR(128),
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS product_favorite (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  user_id BIGINT NOT NULL,
  product_id BIGINT NOT NULL,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  UNIQUE(user_id, product_id)
);

CREATE TABLE IF NOT EXISTS trade_order (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  order_no VARCHAR(128) NOT NULL UNIQUE,
  product_id BIGINT NOT NULL,
  goods_id BIGINT NOT NULL,
  product_no VARCHAR(128) NOT NULL,
  product_title VARCHAR(128) NOT NULL,
  trade_rule_snapshot VARCHAR(128) NOT NULL,
  buyer_id BIGINT NOT NULL,
  seller_id BIGINT NOT NULL,
  amount DECIMAL(18,2) NOT NULL,
  order_status VARCHAR(32) NOT NULL DEFAULT 'PENDING_PAY',
  accepted_trade_rule BOOLEAN NOT NULL DEFAULT FALSE,
  paid_user_id BIGINT,
  ledger_no VARCHAR(128),
  balance_type VARCHAR(32),
  balance_before DECIMAL(18,2),
  balance_after DECIMAL(18,2),
  paid_at TIMESTAMP,
  shipped_at TIMESTAMP,
  shipped_by BIGINT,
  shipping_type VARCHAR(32),
  shipping_company VARCHAR(64),
  tracking_no VARCHAR(128),
  shipping_remark VARCHAR(255),
  completed_at TIMESTAMP,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS order_review (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  review_no VARCHAR(128) NOT NULL UNIQUE,
  order_no VARCHAR(128) NOT NULL,
  reviewer_id BIGINT NOT NULL,
  reviewee_id BIGINT NOT NULL,
  description_score INT NOT NULL,
  service_score INT NOT NULL,
  shipping_score INT NOT NULL,
  content VARCHAR(512) NOT NULL,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  UNIQUE(order_no, reviewer_id)
);

CREATE TABLE IF NOT EXISTS payment_recharge_order (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  recharge_no VARCHAR(128) NOT NULL UNIQUE,
  user_id BIGINT NOT NULL,
  amount DECIMAL(18,2) NOT NULL,
  channel VARCHAR(32) NOT NULL,
  status VARCHAR(32) NOT NULL DEFAULT 'PENDING',
  ledger_no VARCHAR(128),
  balance_before DECIMAL(18,2),
  balance_after DECIMAL(18,2),
  paid_at TIMESTAMP,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS wallet_account (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  user_id BIGINT NOT NULL UNIQUE,
  recharge_balance DECIMAL(18,2) NOT NULL DEFAULT 0,
  income_balance DECIMAL(18,2) NOT NULL DEFAULT 0,
  frozen_balance DECIMAL(18,2) NOT NULL DEFAULT 0,
  withdrawable_balance DECIMAL(18,2) NOT NULL DEFAULT 0,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS wallet_ledger_entry (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  ledger_no VARCHAR(128) NOT NULL UNIQUE,
  user_id BIGINT NOT NULL,
  direction VARCHAR(16) NOT NULL,
  amount DECIMAL(18,2) NOT NULL,
  biz_type VARCHAR(64) NOT NULL,
  biz_no VARCHAR(128),
  balance_type VARCHAR(32) NOT NULL,
  balance_before DECIMAL(18,2) NOT NULL,
  balance_after DECIMAL(18,2) NOT NULL,
  status VARCHAR(32) NOT NULL DEFAULT 'SUCCESS',
  idempotency_key VARCHAR(160) NOT NULL UNIQUE,
  remark VARCHAR(255),
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS payout_account (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  user_id BIGINT NOT NULL,
  payment_method VARCHAR(32) NOT NULL,
  account_name VARCHAR(128) NOT NULL,
  account_no VARCHAR(255) NOT NULL,
  masked_account_no VARCHAR(255) NOT NULL,
  verify_status VARCHAR(32) NOT NULL DEFAULT 'VERIFIED',
  is_default BOOLEAN NOT NULL DEFAULT TRUE,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS withdrawal_record (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  withdrawal_no VARCHAR(128) NOT NULL UNIQUE,
  audit_no VARCHAR(128),
  user_id BIGINT NOT NULL,
  amount DECIMAL(18,2) NOT NULL,
  payment_method VARCHAR(32) NOT NULL,
  account_name VARCHAR(128) NOT NULL,
  account_no VARCHAR(255) NOT NULL,
  status VARCHAR(32) NOT NULL DEFAULT 'PENDING',
  remark VARCHAR(255),
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  reviewed_at TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS im_conversation (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  conversation_no VARCHAR(128) NOT NULL UNIQUE,
  owner_user_id BIGINT NOT NULL,
  peer_user_id BIGINT NOT NULL,
  conversation_type VARCHAR(32) NOT NULL DEFAULT 'SINGLE',
  last_seq BIGINT NOT NULL DEFAULT 0,
  last_message_summary VARCHAR(255),
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS im_message (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  message_no VARCHAR(128) NOT NULL UNIQUE,
  conversation_id BIGINT NOT NULL,
  conversation_no VARCHAR(128) NOT NULL,
  server_seq BIGINT NOT NULL,
  client_msg_id VARCHAR(64) NOT NULL,
  client_key VARCHAR(255) NOT NULL UNIQUE,
  sender_id BIGINT NOT NULL,
  receiver_id BIGINT NOT NULL,
  message_type VARCHAR(32) NOT NULL,
  content_json TEXT NOT NULL,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS im_receipt (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  conversation_id BIGINT NOT NULL,
  user_id BIGINT NOT NULL,
  read_seq BIGINT NOT NULL DEFAULT 0,
  delivered_seq BIGINT NOT NULL DEFAULT 0,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  UNIQUE(conversation_id, user_id)
);

CREATE TABLE IF NOT EXISTS gift_order (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  gift_order_no VARCHAR(128) NOT NULL UNIQUE,
  idempotency_key VARCHAR(255) NOT NULL UNIQUE,
  sender_id BIGINT NOT NULL,
  receiver_id BIGINT NOT NULL,
  gift_id BIGINT NOT NULL,
  gift_code VARCHAR(64) NOT NULL,
  quantity INT NOT NULL,
  total_amount DECIMAL(18,2) NOT NULL,
  platform_share DECIMAL(18,2) NOT NULL,
  receiver_amount DECIMAL(18,2) NOT NULL,
  debit_ledger_no VARCHAR(128) NOT NULL,
  receiver_credit_ledger_no VARCHAR(128) NOT NULL,
  status VARCHAR(32) NOT NULL DEFAULT 'SUCCESS',
  request_no VARCHAR(128),
  client_gift_id VARCHAR(128),
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS community_post (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  post_no VARCHAR(128) NOT NULL UNIQUE,
  author_id BIGINT NOT NULL,
  title VARCHAR(128) NOT NULL,
  topic VARCHAR(64) NOT NULL,
  content TEXT NOT NULL,
  image_urls TEXT,
  status VARCHAR(32) NOT NULL DEFAULT 'PUBLISHED',
  like_count INT NOT NULL DEFAULT 0,
  comment_count INT NOT NULL DEFAULT 0,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS community_comment (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  comment_no VARCHAR(128) NOT NULL UNIQUE,
  post_id BIGINT NOT NULL,
  author_id BIGINT NOT NULL,
  content VARCHAR(512) NOT NULL,
  status VARCHAR(32) NOT NULL DEFAULT 'PUBLISHED',
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS community_like (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  post_id BIGINT NOT NULL,
  user_id BIGINT NOT NULL,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  UNIQUE(post_id, user_id)
);

CREATE TABLE IF NOT EXISTS media_upload_ticket (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  ticket_no VARCHAR(128) NOT NULL UNIQUE,
  owner_user_id BIGINT NOT NULL,
  scene VARCHAR(64) NOT NULL,
  original_filename VARCHAR(255),
  content_type VARCHAR(64) NOT NULL,
  file_size BIGINT NOT NULL,
  storage_url VARCHAR(512) NOT NULL UNIQUE,
  upload_token_hash VARCHAR(128) NOT NULL,
  status VARCHAR(32) NOT NULL DEFAULT 'ISSUED',
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  expires_at TIMESTAMP NOT NULL
);

CREATE TABLE IF NOT EXISTS audit_record (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  audit_no VARCHAR(128) NOT NULL UNIQUE,
  audit_type VARCHAR(64) NOT NULL,
  user_id BIGINT NOT NULL,
  target_type VARCHAR(64) NOT NULL,
  target_id VARCHAR(128) NOT NULL,
  reason VARCHAR(128) NOT NULL,
  description VARCHAR(512),
  status VARCHAR(32) NOT NULL DEFAULT 'PENDING',
  review_remark VARCHAR(512),
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  reviewed_at TIMESTAMP
);

CREATE TABLE IF NOT EXISTS admin_audit_log (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  action VARCHAR(64) NOT NULL,
  operator_id BIGINT NOT NULL,
  target_type VARCHAR(64) NOT NULL,
  target_id VARCHAR(128) NOT NULL,
  result VARCHAR(32) NOT NULL,
  summary VARCHAR(512),
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX IF NOT EXISTS idx_admin_audit_log_created ON admin_audit_log(created_at, id);

CREATE TABLE IF NOT EXISTS report_record (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  report_no VARCHAR(128) NOT NULL UNIQUE,
  reporter_id BIGINT NOT NULL,
  target_type VARCHAR(64) NOT NULL,
  target_id VARCHAR(128) NOT NULL,
  reason_code VARCHAR(64) NOT NULL,
  description VARCHAR(512),
  report_status VARCHAR(32) NOT NULL DEFAULT 'PENDING',
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  handled_at TIMESTAMP
);

CREATE TABLE IF NOT EXISTS notification_record (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  notification_no VARCHAR(128) NOT NULL UNIQUE,
  user_id BIGINT NOT NULL,
  notification_type VARCHAR(32) NOT NULL,
  title VARCHAR(80) NOT NULL,
  description VARCHAR(300) NOT NULL,
  target_url VARCHAR(256),
  read_flag BOOLEAN NOT NULL DEFAULT FALSE,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  read_at TIMESTAMP
);

CREATE INDEX IF NOT EXISTS idx_notification_record_user_type ON notification_record(user_id, notification_type, created_at);

CREATE TABLE IF NOT EXISTS after_sales_record (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  after_sales_no VARCHAR(128) NOT NULL UNIQUE,
  order_no VARCHAR(128) NOT NULL,
  applicant_id BIGINT NOT NULL,
  after_sales_type VARCHAR(32) NOT NULL,
  refund_amount DECIMAL(18,2) NOT NULL,
  reason VARCHAR(64) NOT NULL,
  description VARCHAR(512) NOT NULL,
  evidence_urls TEXT,
  after_sales_status VARCHAR(32) NOT NULL DEFAULT 'PENDING_REVIEW',
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  reviewed_at TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  UNIQUE(order_no, applicant_id)
);
